# LLM / API keys (only required if you use these integrations)
OPENAI_API_KEY=sk-proj-...
TAVILY_API_KEY=tvly-...

# Neo4j connection settings (when running with docker-compose these defaults work)
NEO4J_URI=bolt://neo4j:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=personamate
NEO4J_DB=neo4j

# Optional: configure PYTHONPATH used by the backend container
# (docker-compose sets this automatically; only needed for local dev)
# PYTHONPATH=/app/src/python

# Security: do NOT commit real secrets. Copy this file to `.env` and
# replace example values with your actual secrets before running.